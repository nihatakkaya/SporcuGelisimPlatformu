@page "/coach/profile"
@attribute [Authorize(Roles = RoleNames.Coach)]
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("/Account/Manage", replace: true);
    }
}
